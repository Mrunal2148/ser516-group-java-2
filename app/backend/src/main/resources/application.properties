
# =========================================
# 🌐 Server Configuration
# =========================================
server.port=8080
spring.application.name=DefectsRemovedService

# =========================================
# 🔑 GitHub API Configuration
# =========================================
# Replace with your actual GitHub token
github.token=${GITHUB_TOKEN}

# =========================================
# 🔧 Logging Configuration
# =========================================
# Log all requests (useful for debugging)
logging.level.org.springframework.web=DEBUG
logging.level.com.myproject=DEBUG

# =========================================
# 🚀 Spring Boot Actuator (For Debugging)
# =========================================
management.endpoints.web.exposure.include=*
management.endpoint.health.show-details=always

# =========================================
# 🏗 Hibernate / JPA Settings (If Using Database)
# =========================================
# Uncomment and configure if using a database

# spring.datasource.url=jdbc:mysql://localhost:3306/your_database
# spring.datasource.username=root
# spring.datasource.password=your_password
# spring.jpa.hibernate.ddl-auto=update
# spring.jpa.show-sql=true

# =========================================
# 📡 CORS Configuration (Optional: Allow API Access from Frontend)
# =========================================
# spring.web.cors.allowed-origins=*
# spring.web.cors.allowed-methods=*
# spring.web.cors.allowed-headers=*
